// Mobile-only because tap targets are a property of fingers, not web layout.

// Minimum size for anything intended to receive a finger.
export const minTouchTarget = 44;

// Shared height for Input and the default Button.
export const controlHeight = 48;
