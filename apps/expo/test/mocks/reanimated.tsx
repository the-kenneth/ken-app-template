// expo-router replaces this module with `react-native-reanimated/mock`, so the
// implementation must live one module away to avoid a self-replacing mock.
export { default } from "./reanimated-fake";
export * from "./reanimated-fake";
